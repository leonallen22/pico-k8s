network:
  version: 2
  ethernets:
    {{ ansible_default_ipv4.interface }}:
      dhcp4: no
      addresses:
        - {{ static_ip }}/{{ netmask }}
      gateway4: {{ gateway }}
      nameservers:
        addresses: [{{ dns_servers | join(', ') }}]
